diff --git a/node_modules/lz4/lib/binding/lz4_binding.cc b/node_modules/lz4/lib/binding/lz4_binding.cc
index 24fe139..098ed09 100644
--- a/node_modules/lz4/lib/binding/lz4_binding.cc
+++ b/node_modules/lz4/lib/binding/lz4_binding.cc
@@ -342,6 +342,42 @@ NAN_METHOD(LZ4Uncompress) {
   info.GetReturnValue().Set(result);
 }
 
+// {Buffer} input, {Buffer} output, {Buffer} dict (optional)
+NAN_METHOD(LZ4UncompressWithDict) {
+  Nan::HandleScope scope;
+
+  if (info.Length() < 2 || info.Length() > 3) {
+    Nan::ThrowError("Wrong number of arguments: need (input, output[, dict])");
+    return;
+  }
+
+  if (!Buffer::HasInstance(info[0]) || !Buffer::HasInstance(info[1])) {
+    Nan::ThrowTypeError("Wrong arguments");
+    return;
+  }
+  Local<Object> input = Local<Object>::Cast(info[0]);
+  Local<Object> output = Local<Object>::Cast(info[1]);
+
+  const char* dictStart = nullptr;
+  int dictSize = 0;
+  if (info.Length() >= 3 && Buffer::HasInstance(info[2])) {
+    Local<Object> dict = Local<Object>::Cast(info[2]);
+    dictStart = Buffer::Data(dict);
+    dictSize = Buffer::Length(dict);
+  }
+
+  int result = LZ4_decompress_safe_usingDict(
+    Buffer::Data(input),
+    Buffer::Data(output),
+    Buffer::Length(input),
+    Buffer::Length(output),
+    dictStart,
+    dictSize
+  );
+
+  info.GetReturnValue().Set(Nan::New<Integer>(result));
+}
+
 // {Buffer} input, {Buffer} output
 NAN_METHOD(LZ4Uncompress_fast) {
   Nan::HandleScope scope;
@@ -380,6 +416,7 @@ NAN_MODULE_INIT(init_lz4) {
   Nan::Export(target, "compressHCLimited", LZ4CompressHCLimited);
 
   Nan::Export(target, "uncompress", LZ4Uncompress);
+  Nan::Export(target, "uncompressWithDict", LZ4UncompressWithDict);
   Nan::Export(target, "uncompress_fast", LZ4Uncompress_fast);
 }
 
diff --git a/node_modules/lz4/lib/lz4.js b/node_modules/lz4/lib/lz4.js
index ef94901..a090fa0 100644
--- a/node_modules/lz4/lib/lz4.js
+++ b/node_modules/lz4/lib/lz4.js
@@ -17,6 +17,7 @@ module.exports.encode = require('./encoder').LZ4_compress
 var bindings = module.exports.utils.bindings
 
 module.exports.decodeBlock = bindings.uncompress
+module.exports.decodeBlockWithDict = bindings.uncompressWithDict
 
 module.exports.encodeBound = bindings.compressBound
 module.exports.encodeBlock = bindings.compress
